1. Реализовала аутентификацию пользователя через ВКонтакте
     (id приложения и защищённый ключ загружаются из /conf/local.conf)

2. Поработала со связью моделей «один-к-одному»:
    создала профиль пользователя models -> ShopUserProfile,
    обновила уже существующих пользователей -> /mainapp/management/update_db.py,
    добавила форму для редактирования forms -> ShopUserProfileEditForm,
    обновила контролер /authapp/edit_view

    Делала по методичке и по записи вебинара, всё работает, но как - до конца ещё не разобралась.

3. Реализовала автоматическое заполнение профиля пользователя при аутентификации через социальную сеть ->
    scope, pipeline.

4. Задала при проверке минимальный возраст 100 лет для проверки исключения pipeline.py -> AuthForbidden
    (выключила в urls.py -> path('__debug__/', include(debug_toolbar.urls)), в local.conf -> Debug=False).
    Перебросило на главную страницу, пользователь не создался.

Можно ли как-то удалять автоматически аватарки из media, когда удаляется пользователь?